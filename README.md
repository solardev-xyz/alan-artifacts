# alan-artifacts

Public host for screenshots and other evidence that
[alan](https://github.com/solardev-xyz/alan) posts into PR comments
(GitHub can't attach images via API, and raw URLs must be public to render).

Layout: `<owner>/<repo>/pr<N>/<timestamp>-<label>.png`

Contents are disposable evidence, not source. Old directories may be pruned.
